<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:wfc="http://www.springframework.org/schema/wfc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/wfc
       http://www.springframework.org/schema/wfc.xsd ">

    <bean id="deepLinkMsgPusher" class="cc.linkedme.mcq.DeepLinkMsgPusher" />

    <bean id="deepLinkDao" class="cc.linkedme.dao.sdkapi.impl.DeepLinkDaoImpl">
        <property name="tableContainer">
            <bean class="cc.linkedme.data.dao.strategy.TableContainer">
                <property name="tablesItems">
                    <map>
                        <entry key="deeplink" value-ref="deeplink" />
                    </map>
                </property>
            </bean>
        </property>
    </bean>

    <bean id="deeplink" class="cc.linkedme.data.dao.strategy.TableItem">
        <property name="itemName" value="deeplink"/>
        <property name="dbNamePrefix"  value="deeplink"/>
        <property name="dbCount"  value="4"/>

        <property name="tableNamePrefix" value="deeplink_info" />
        <property name="tableNamePostFix" value="yymm" />  <!-- 默认是索引 -->
        <property name="tableCount" value="1" />           <!-- 这个参数和tableNamePostFix有关系，如果是日期就不用了。 -->
        <property name="sqls">
            <map>
                <entry key="ADD_DEEPLINK" value="insert into $db$.$tb$ (deeplink_id, deeplink_md5, linkedme_key, identity_id, appid, create_time, tags, alias, channel, feature, stage, campaign, source, sdk_version, update_time) values(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, now())"/>
                <entry key="GET_DEEPLINK_INFO" value="select deeplink_id, create_time, tags, alias, channel, feature, stage, campaign, source from $db$.$tb$ where appid = ?" />
                <entry key="GET_DEEPLINKS" value="select deeplink_id, create_time, tags, alias, channel, feature, stage, campaign, source from $db$.$tb$ where deeplink_id = ? and appid = ?" />
                <entry key="UPDATE_DEEPLINKS" value="update $db$.$tb$ set vflag = ?, mflag=? where status_id = ? and vflag in (0,6,7) and cmt_id = ?"/>
            </map>
        </property>
        <property name="jdbcTemplates">
            <list>
                <ref bean="deeplink_jdbctemplate_0"/>
                <!--<ref bean="deeplink_jdbctemplate_1"/> -->
            </list>
        </property>
    </bean>

    <bean id="deepLinkParamDao" class="cc.linkedme.dao.sdkapi.impl.DeepLinkParamDaoImpl">
        <property name="tableContainer">
            <bean class="cc.linkedme.data.dao.strategy.TableContainer">
                <property name="tablesItems">
                    <map>
                        <entry key="deepLinkParam" value-ref="deepLinkParam" />
                    </map>
                </property>
            </bean>
        </property>
    </bean>

    <bean id="deepLinkParam" class="cc.linkedme.data.dao.strategy.TableItem">
        <property name="itemName" value="deeplinkParam"/>
        <property name="dbNamePrefix"  value="deeplink_param"/>
        <property name="dbCount"  value="4"/>

        <property name="tableNamePrefix" value="deeplink_param_info" />
        <!-- <property name="tableNamePostFix" value="yymm" /> --> <!-- 默认是索引 -->
        <property name="tableCount" value="8" />           <!-- 这个参数和tableNamePostFix有关系，如果是日期就不用了。 -->
        <property name="sqls">
            <map>
                <entry key="ADD_DEEPLINK_PARAM" value="insert into $db$.$tb$ (deeplink_id, param) values(?, ?)"/>
                <entry key="GET_DEEPLINK_PARAM_BY" value="update $db$.$tb$ set vflag = ?, mflag=? where status_id = ? and vflag in (0,6,7) and cmt_id = ?"/>
                <entry key="GET_DEEPLINK_PARAM" value="select param from $db$.$tb$ where deeplink_id=?"/>
            </map>
        </property>
        <property name="jdbcTemplates">
            <list>
                <ref bean="deeplinkparam_jdbctemplate_0"/>
                <!--<ref bean="deeplink_jdbctemplate_1"/> -->
            </list>
        </property>
    </bean>

    <bean id="deepLinkCountComponent"
          class="cc.linkedme.commons.counter.component.impl.CountComponentSimple">
        <property name="daoEnable" value="true" />
        <property name="countDao" ref="deepLinkCountDao" />
    </bean>

</beans>